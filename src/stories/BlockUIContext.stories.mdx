import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { BlockUI } from '../BlockUI';

<Meta title="Documents/BlockUI Context" component={BlockUI} />

# BlockUI Context

With `MDX`, we can define a story for `BlockUI` right in the middle of our Markdown documentation.